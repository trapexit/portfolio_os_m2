#include <hardware/PPCMacroequ.i>

	DECFN main
	blr

	DECFN CreateIFFParser
	blr

	DECFN DeleteIFFParser
	blr

	DECFN ParseIFF
	blr

	DECFN ReadChunk
	blr

	DECFN WriteChunk
	blr

	DECFN PushChunk
	blr

	DECFN PopChunk
	blr

	DECFN RegisterPropChunks
	blr

	DECFN RegisterStopChunks
	blr

	DECFN RegisterCollectionChunks
	blr

	DECFN FindPropChunk
	blr

	DECFN FindCollection
	blr

	DECFN FindPropContext
	blr

	DECFN GetCurrentContext
	blr

	DECFN GetParentContext
	blr

	DECFN AllocContextInfo
	blr

	DECFN FreeContextInfo
	blr

	DECFN FindContextInfo
	blr

	DECFN StoreContextInfo
	blr

	DECFN AttachContextInfo
	blr

	DECFN InstallEntryHandler
	blr

	DECFN InstallExitHandler
	blr

	DECFN SeekChunk
	blr

	DECFN GetIFFOffset
	blr

	DECFN CreateIFFParserVA
	blr

	DECFN RemoveContextInfo
	blr

	DECFN ReadChunkCompressed
	blr

	DECFN WriteChunkCompressed
	blr

