#include <hardware/PPCMacroequ.i>

	DECFN main
	blr

	DECFN PixelAddress
	blr

	DECFN ModifyGraphicsItem
	blr

	DECFN SuperModifyGraphicsItem
	blr

	DECFN AddViewToViewList
	blr

	DECFN SuperAddViewToViewList
	blr

	DECFN RemoveView
	blr

	DECFN SuperRemoveView
	blr

	DECFN OrderViews
	blr

	DECFN SuperOrderViews
	blr

	DECFN LockDisplay
	blr

	DECFN SuperLockDisplay
	blr

	DECFN UnlockDisplay
	blr

	DECFN SuperUnlockDisplay
	blr

	DECFN SuperInternalCompile
	blr

	DECFN InternalPixelAddr
	blr

	DECFN SuperInternalMarkView
	blr

	DECFN QueryGraphics
	blr

	DECFN NextViewTypeInfo
	blr

	DECFN NextProjector
	blr

	DECFN ActivateProjector
	blr

	DECFN SuperActivateProjector
	blr

	DECFN DeactivateProjector
	blr

	DECFN SuperDeactivateProjector
	blr

	DECFN SetDefaultProjector
	blr

	DECFN SuperSetDefaultProjector
	blr

	DECFN SuperInternalSetRendCallBack
	blr

	DECFN SuperInternalSetDispCallBack
	blr

