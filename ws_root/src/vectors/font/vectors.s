#include <hardware/PPCMacroequ.i>

	DECFN main
	blr

	DECFN CreateTextState
	blr

	DECFN DeleteTextState
	blr

	DECFN DrawText
	blr

	DECFN DrawString
	blr

	DECFN GetStringExtent
	blr

	DECFN GetCharacterData
	blr

	DECFN MoveText
	blr

	DECFN GetTextPosition
	blr

	DECFN ScaleText
	blr

	DECFN GetTextScale
	blr

	DECFN RotateText
	blr

	DECFN GetTextAngle
	blr

	DECFN GetTextExtent
	blr

	DECFN TrackTextBounds
	blr

	DECFN SetTextColor
	blr

	DECFN SetClipBox
	blr

	DECFN TransformText
	blr

	DECFN CreateTextStencil
	blr

	DECFN DrawTextStencil
	blr

	DECFN GetTextStateFromStencil
	blr

	DECFN DeleteTextStencil
	blr

