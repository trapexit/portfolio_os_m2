#include <hardware/PPCMacroequ.i>

	DECFN main
	blr

	DECFN GetDecompressorWorkBufferSize
	blr

	DECFN FeedDecompressor
	blr

	DECFN DeleteDecompressor
	blr

	DECFN CreateDecompressor
	blr

	DECFN GetCompressorWorkBufferSize
	blr

	DECFN FeedCompressor
	blr

	DECFN DeleteCompressor
	blr

	DECFN CreateCompressor
	blr

	DECFN CreateCompressorVA
	blr

	DECFN CreateDecompressorVA
	blr

	DECFN GetCompressorWorkBufferSizeVA
	blr

	DECFN GetDecompressorWorkBufferSizeVA
	blr

	DECFN SimpleCompress
	blr

	DECFN SimpleDecompress
	blr

