# @(#) Makefile 96/05/19 1.13

TARGET_DIRECTORY =	${MODULE_DIR}
LOADERTOOL_FLAGS =	-Htype=4 -e main
SIGNED_MODULE = 	1
NO_CHDIR_MODULE =	1
SHOW_MODULE_INFO =	1

CDEFINES =		-DVECTORSTART=${VECTORSTART} \
			-DVECTORSIZE=${VECTORSIZE} \
			-DBOOTDATASTART=${BOOTDATASTART} \
			-DBOOTDATASIZE=${BOOTDATASIZE} \
			-DDIPIRDATASTART=${DIPIRDATASTART} \
			-DDIPIRDATASIZE=${DIPIRDATASIZE} \
			-DDIPIRBUFSTART=${DIPIRBUFSTART} \
			-DDIPIRBUFSIZE=${DIPIRBUFSIZE} \
			-DDEBUGGERSTART=${DEBUGGERSTART} \
			-DDEBUGGERSIZE=${DEBUGGERSIZE}

include ${CODEMGR_WS}/ws_root/variables/linkexec
include ${CODEMGR_WS}/ws_root/rules/linkexec
include ${CODEMGR_WS}/ws_root/variables/memorymap
